{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <div class="menu"><a href="{{ path('app_post_create') }}">Ajouter</a></div>

    <article class="post">
        <h1>{{ post.title | upper }}</h1>
        <p>Publié le {{ post.createdAt | date('d/m/Y') }} par {{ post.writtenBy.firstName }}</p>

        <div class="body">
            {{ post.body }}
        </div>
    </article>
{% endblock %}
