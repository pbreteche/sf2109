{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% dump 'tag twig' %}

{% block body %}
<div class="menu"><a href="{{ path('app_post_create') }}">Ajouter</a></div>

<article class="post">
    <h1>{{ post.title }}</h1>

    <p>{{ dump('fonction dump') }}</p>

    <div class="body">
        {{ post.body }}
    </div>
</article>
{% endblock %}
